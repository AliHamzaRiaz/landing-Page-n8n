# Frontend (Vite) — safe to expose in the browser
VITE_API_URL=http://localhost:3001/api

# Optional: Meta App ID for Facebook SDK init (same as backend META_APP_ID)
# Embedded Signup config ID is fetched from the backend at runtime.
VITE_META_APP_ID=
